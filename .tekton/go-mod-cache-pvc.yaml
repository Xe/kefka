apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: go-mod-cache
  namespace: ci
spec:
  resources:
    requests:
      storage: 16Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
