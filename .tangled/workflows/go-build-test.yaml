when:
  - event: ["push"]
    branch: ["main"]
  - event: ["pull_request"]
    branch: ["main"]

engine: nixery

tack:
  tekton:
    pipeline: kefka-build-test
    workspaces:
      - name: repo-data
        accessModes: ["ReadWriteOnce"]
        storage: 1Gi
